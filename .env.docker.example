# Hope Platform Docker - Environment Variables Template
# Copy this file to .env.docker and fill in actual values
# Then run: docker-compose --env-file .env.docker up
# NEVER commit .env.docker to version control!

# PostgreSQL Configuration
POSTGRES_DB=hope_db
POSTGRES_USER=hope_user
POSTGRES_PASSWORD=your-secure-database-password-here

# Security - GENERATE STRONG RANDOM KEYS
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=your-secret-key-here-64-chars-minimum
ENCRYPTION_KEY=your-encryption-key-here-64-chars-minimum

# Environment (development or production)
ENVIRONMENT=development

# CORS - Allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:8081,exp://localhost:19000,http://localhost:3000
